
import React from 'react';
import Index from './Index';

// This is a simple wrapper around Index for backward compatibility
const Dashboard = () => {
  return <Index />;
};

export default Dashboard;
